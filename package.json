{
  "name": "beoneofus-web",
  "version": "0.1.0",
  "private": true,
  "description": "beoneofus — public 'Global Opportunity Ecosystem' web app (apps/web). apps/core now lives independently in its own project.",
  "scripts": {
    "dev": "npm --prefix apps/web run dev",
    "build": "npm --prefix apps/web run build",
    "test": "npm --prefix apps/web run test:e2e",
    "lint": "npm --prefix apps/web run lint",
    "install:all": "npm --prefix apps/web install"
  },
  "engines": {
    "node": ">=20"
  }
}
